// App is deliberately only the application entry composition point.
// Route definitions, session state, and access checks live in their focused modules.
import { PortalRouter } from './routes/PortalRouter'

export default PortalRouter
